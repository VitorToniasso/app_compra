import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 342,
    height: 48,
    backgroundColor: "#fff",
    borderRadius: 8,
    paddingHorizontal: 16,
    marginTop: 42,
    marginBottom: 12,
  },
});
